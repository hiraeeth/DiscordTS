services:
  app:
    build: .
    image: discordbot:latest
    container_name: discordbot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"
